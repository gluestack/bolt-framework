container_name: SERVICE_NAME
stateless: false
service_runners:
  docker:
    envfile: .env
    build: ./run.Dockerfile
    healthcheck:
      test: "/etc/init.d/mysql status"
      interval: 1s
      retries: 120
    ports:
      - PORT:3306

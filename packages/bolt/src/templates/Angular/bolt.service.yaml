container_name: SERVICE_NAME
stateless: true
service_runners:
  local:
    envfile: .env
    build: npm install && ng serve
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:4200

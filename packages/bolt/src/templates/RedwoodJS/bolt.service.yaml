container_name: SERVICE_NAME
stateless: true
service_runners:
  local:
    envfile: .env
    build: yarn && yarn rw dev
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:8910

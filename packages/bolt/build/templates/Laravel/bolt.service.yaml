container_name: SERVICE_NAME
stateless: true
service_runners:
  local:
    envfile: .env
    build: composer install && php artisan serve
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:9000

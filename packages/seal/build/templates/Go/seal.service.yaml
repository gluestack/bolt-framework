container_name: SERVICE_NAME
stateless: true
platforms:
  local:
    envfile: .env
    build: go mod download && go run main.go
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:8080

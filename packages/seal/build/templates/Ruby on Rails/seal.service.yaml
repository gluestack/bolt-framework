container_name: SERVICE_NAME
stateless: true
platforms:
  local:
    envfile: .env
    build: bundle install && rails server
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:3000

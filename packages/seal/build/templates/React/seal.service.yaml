container_name: SERVICE_NAME
stateless: true
platforms:
  local:
    envfile: .env
    build: npm install && npm start
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:3000

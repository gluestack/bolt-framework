container_name: SERVICE_NAME
stateless: true
platforms:
  local:
    envfile: .env
    build: shards install && crystal run app.cr
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:3000

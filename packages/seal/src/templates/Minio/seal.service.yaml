container_name: SERVICE_NAME
stateless: false
platforms:
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - MINIO_PORT:9000
      - CONSOLE_PORT:9001
    volumes:
      - DIRECTORY_PATH/data:/data
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://SERVICE_NAME:9000/minio/health/live
      interval: 5s
      timeout: 2s
      retries: 20

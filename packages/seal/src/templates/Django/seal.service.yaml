container_name: SERVICE_NAME
stateless: true
platforms:
  local:
    envfile: .env
    build: pip install -r requirements.txt && python manage.py runserver
  docker:
    envfile: .env
    build: ./run.Dockerfile
    ports:
      - PORT:8000
